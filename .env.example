VAULT_PATH=
OPENCODE_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_ALLOWED_USER_IDS=
# Shared secret for one-tap spend ingress (POST /api/spend). Used by the iOS Shortcut.
QUICK_LOG_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Set false on Vercel so only an external trusted worker drains agent_runs.
DASH_AGENT_AUTORUN=
# Worker polling and timeout knobs for npm run agent:run.
AGENT_RUNNER_INTERVAL_MS=
AGENT_RUNNER_TIMEOUT_MS=
# Auth bypass is controlled by AUTH_BYPASS_ENABLED in lib/flags.ts.
GROQ_API_KEY=
HERMES_REMINDER_WEBHOOK_URL=
HERMES_REMINDER_WEBHOOK_SECRET=
